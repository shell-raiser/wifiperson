name: Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: build-main
  cancel-in-progress: true

jobs:
  linux:
    name: Linux AppImage and tarball
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            libnl-3-dev \
            libnl-genl-3-dev \
            ninja-build \
            pkg-config \
            wget

      - name: Install Qt
        uses: jurplel/install-qt-action@v4.3.0
        with:
          version: '6.10.3'
          host: linux
          target: desktop
          arch: gcc_64
          modules: qtcharts

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_PREFIX_PATH="${QT_ROOT_DIR}"

      - name: Build
        run: cmake --build build --parallel

      - name: Stage normal build
        run: |
          DESTDIR="$PWD/package-root" cmake --install build --strip
          mkdir -p dist
          tar -C package-root -czf dist/wifiperson-linux-x86_64-build.tar.gz .

      - name: Download AppImage tools
        run: |
          wget -O linuxdeploy-x86_64.AppImage https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -O linuxdeploy-plugin-qt-x86_64.AppImage https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          wget -O appimagetool-x86_64.AppImage https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
          chmod +x ./*.AppImage

      - name: Build AppImage
        env:
          APPIMAGE_EXTRACT_AND_RUN: 1
          OUTPUT: wifiperson-linux-x86_64.AppImage
        run: |
          rm -rf AppDir
          DESTDIR="$PWD/AppDir" cmake --install build --strip
          export PATH="$PWD:$PATH"
          export QMAKE="$(command -v qmake6 || command -v qmake)"
          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --plugin qt \
            --output appimage
          mv "${OUTPUT}" "dist/${OUTPUT}"

      - name: Upload latest build
        uses: actions/upload-artifact@v7
        with:
          name: linux-build
          path: dist/*
          retention-days: 7
          if-no-files-found: error

      - name: Delete older build artifacts
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const currentRunId = context.runId;
            const artifactNames = new Set(['linux-build', 'windows-build']);
            const artifacts = await github.paginate(github.rest.actions.listArtifactsForRepo, {
              owner,
              repo,
              per_page: 100,
            });

            for (const artifact of artifacts) {
              if (artifactNames.has(artifact.name) && artifact.workflow_run?.id && artifact.workflow_run.id !== currentRunId) {
                await github.rest.actions.deleteArtifact({
                  owner,
                  repo,
                  artifact_id: artifact.id,
                });
              }
            }

  windows:
    name: Windows EXE and installer
    runs-on: windows-2022

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Qt
        uses: jurplel/install-qt-action@v4.3.0
        with:
          version: '6.10.3'
          host: windows
          target: desktop
          arch: win64_msvc2022_64
          modules: qtcharts
          tools: tools_ifw

      - name: Configure
        run: cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}\windows-package"

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Stage Windows bundle
        shell: pwsh
        run: |
          cmake --install build --config Release
          windeployqt --release --compiler-runtime windows-package\bin\wifiperson.exe
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Compress-Archive -Path windows-package\* -DestinationPath dist\wifiperson-windows-x86_64-exe.zip -Force

      - name: Build installer
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item -Recurse -Force packaging\ifw\packages installer-packages
          New-Item -ItemType Directory -Force -Path installer-packages\io.github.wifiperson.wifiperson\data | Out-Null
          Copy-Item -Recurse -Force windows-package\* installer-packages\io.github.wifiperson.wifiperson\data
          binarycreator --offline-only `
            -c packaging\ifw\config\config.xml `
            -p installer-packages `
            dist\wifiperson-windows-x86_64-installer.exe

      - name: Upload latest Windows build
        uses: actions/upload-artifact@v7
        with:
          name: windows-build
          path: dist/*
          retention-days: 7
          if-no-files-found: error

      - name: Delete older build artifacts
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const currentRunId = context.runId;
            const artifactNames = new Set(['linux-build', 'windows-build']);
            const artifacts = await github.paginate(github.rest.actions.listArtifactsForRepo, {
              owner,
              repo,
              per_page: 100,
            });

            for (const artifact of artifacts) {
              if (artifactNames.has(artifact.name) && artifact.workflow_run?.id && artifact.workflow_run.id !== currentRunId) {
                await github.rest.actions.deleteArtifact({
                  owner,
                  repo,
                  artifact_id: artifact.id,
                });
              }
            }

  release:
    name: Publish commit release
    runs-on: ubuntu-24.04
    needs:
      - linux
      - windows

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          path: release-assets

      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          TAG: build-${{ github.sha }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          mapfile -t files < <(find release-assets -type f | sort)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No build files were downloaded."
            exit 1
          fi

          short_sha="${COMMIT_SHA:0:12}"
          release_title="Build ${short_sha}"
          release_notes="Automated build for commit ${COMMIT_SHA}."

          if gh release view "${TAG}" --repo "${REPO}" >/dev/null 2>&1; then
            gh release upload "${TAG}" "${files[@]}" --repo "${REPO}" --clobber
          else
            gh release create "${TAG}" "${files[@]}" \
              --repo "${REPO}" \
              --target "${COMMIT_SHA}" \
              --title "${release_title}" \
              --notes "${release_notes}"
          fi
